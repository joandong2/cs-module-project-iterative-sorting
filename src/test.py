# declare the array
arr = [[8, 4], [90, -1, 3], [9, 62], [-7, -1, -56, -6], [201], [76, 18]]
# declare another for the minimum value
temp_arr = []
# loop and find the minimim value for each of the array
for i in range(len(arr)):

    # insert to the new array
    # im going to loop the new array, and sum all of values of those array

    # make a function to get minimum


def get_min(arr):
    for i in range(len(arr)):
        min = 0
        if(arr[i] < arr[min]):

    return min
